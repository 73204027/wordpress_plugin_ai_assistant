# no_specified
Wordpress points to External API (Can be oficial api's or VPS hosting Ollama)
Requires: 
* 0 additional website slots
* 0 additional node.js web app slots
* 1 additional VPS plan for hosting Ollama
    (watch midudev video about opencode en producción? "Tengo opencode en mi VPS" to identify VPS options)

Uses
/* 
    I think no_specified will be the only one, since we won't use another website or node.js web app, since neither can run cron daemons, which Ollama is.
    => We fucking NEED an external VPS
*/